PACKS="vim wget iputils-ping nano procps libnet-ldap-perl libconvert-asn1-perl"
#Instalación openldap y webmin
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y slapd ldap-utils libnss-ldap libpam-ldap,rm /etc/apt/apt.conf.d/docker-gzip-indexes && apt-get purge apt-show-versions && rm /var/lib/apt/lists/*lz4 && apt-get -o Acquire::GzipIndexes=false update && apt-get install -y apt-show-versions,cd /tmp && wget https://prdownloads.sourceforge.net/webadmin/webmin_1.941_all.deb && apt install -y /tmp/webmin_1.941_all.deb"

