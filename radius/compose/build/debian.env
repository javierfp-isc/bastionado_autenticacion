PACKS="vim iputils-ping curl nano net-tools traceroute dnsutils procps firefox-esr"
#Enrutamiento
COMMANDS="echo ip route del default > /etc/routes,echo ip route add default via 192.168.21.100 >> /etc/routes"

